<h1>Listing metrics</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Value</th>
    <th>Timestamp</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @metrics.each do |metric| %>
  <tr>
    <td><%= metric.name %></td>
    <td><%= metric.value %></td>
    <td><%= metric.timestamp %></td>
    <td><%= link_to 'Show', [@namespace, metric] %></td>
    <td><%= link_to 'Edit', edit_namespace_metric_path(@namespace, metric) %></td>
    <td><%= link_to 'Destroy', [@namespace, metric], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Metric', new_namespace_metric_path(@namespace) %>
